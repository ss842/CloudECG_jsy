from flask import Flask, request, jsonify
app = Flask(__name__)

array = [numbers, 1, 2, 3, 4, 5, 6]

# @app.route("/hello/<age>")
# def hello(age):
#     try:
#         age_int = int(age)
#     except ValueError:
#         return send_error("Could not convert")
#     return_str = "you are %d years old" % age_int
#     return jsonify(return_str)
